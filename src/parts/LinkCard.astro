---
import { Icon } from 'astro-icon/components';
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<"a"> {
  icon?: string;
}

const {icon, ...attrs} = Astro.props;
---

<a class="card row" {...attrs}>
  <slot />
  {icon && <Icon name={icon} />}
</a>
